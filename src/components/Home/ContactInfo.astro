---
import SVG from "@/components/SVG.astro";
---

<p>
  ご依頼はこちらから。UIやロゴなどのデザイン制作、コーディング、イベントでの登壇等を受け付けております。
</p>
<ul>
  <li>
    <a href="/price" class="c-card">
      <SVG symbol="price" />
      <h3>PRICE</h3>
      <p>請負内容・参考価格</p>
      <SVG symbol="next" />
    </a>
  </li>
  <li>
    <a href="/contact" class="c-card">
      <SVG symbol="contact" />
      <h3>CONTACT</h3>
      <p>お問い合わせ</p>
      <SVG symbol="next" />
    </a>
  </li>
</ul>

<style lang="less">
  @import "@/styles/common.less";

  p {
    margin-top: 16px;
  }

  ul {
    display: flex;
    gap: 16px;
    @media (width < @lg) {
      flex-direction: column;
      gap: 0;
    }
  }
  li {
    margin-top: 16px;
    width: 100%;
  }
  a {
    display: grid;
    grid-template-columns: 32px 1fr 32px;
    gap: 2px 16px;
    padding: 16px 16px 16px 24px;
    align-items: center;
    background-color: rgba(@main, 0.1);
    :global(svg) {
      width: 32px;
      aspect-ratio: 1 / 1;
      fill: rgb(@theme);
      &:first-child {
        grid-area: 1 / 1 / 3 / 2;
      }
      &:last-child {
        grid-area: 1 / 3 / 3 / 4;
        opacity: 0.2;
      }
    }
    h3 {
      grid-area: 1 / 2 / 2 / 3;
      font-size: 20px;
      letter-spacing: 0.05em;
    }
    p {
      grid-area: 2 / 2 / 3 / 3;
      font-size: 14px;
      opacity: 0.7;
      margin: 0;
    }
  }
</style>
