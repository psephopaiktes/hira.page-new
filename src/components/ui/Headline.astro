---
const { label } = Astro.props;
---

<h1>{label}</h1>

<style lang="less">
  @import "@/styles/common.less";

  h1 {
    margin-top: 48px;
    color: rgb(@label);
    @media (width < @lg) {
      margin-top: 32px;
    }
  }
</style>
